---
description: Append a decision/dead-end to the cross-session worklog (or show recent entries).
argument-hint: "<entry>  | --show [N]"
---
Use the `work-tracking` skill:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/worklog.mjs" $ARGUMENTS
```
